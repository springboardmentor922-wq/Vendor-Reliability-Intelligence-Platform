import AppLayout from "../../components/layout/AppLayout";

export default function VendorListPage() {
  // TODO: fetch from GET /vendors once the vendor router is built on the backend

  return (
    <AppLayout>
      <h1>Vendors</h1>
      <p>Vendor list will show here once the backend /vendors endpoint is ready.</p>
    </AppLayout>
  );
}
